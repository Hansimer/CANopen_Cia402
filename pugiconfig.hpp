// Minimal pugiconfig.hpp stub to allow building bundled pugixml
#ifndef HEADER_PUGICONFIG_HPP
#define HEADER_PUGICONFIG_HPP

// Leave default configuration (no special defines). This file intentionally
// keeps minimal content because the full pugixml config isn't required
// for simple XML parsing used by xml_to_json.cpp.

#endif // HEADER_PUGICONFIG_HPP
